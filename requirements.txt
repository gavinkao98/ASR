# 直接依賴（宣告用）。實際安裝請用 requirements.lock.txt——那份才是經過驗證的版本組合。
#
# 這裡只有預設引擎 Qwen3 的路徑會用到的東西。Breeze 引擎（選用）的依賴另見
# requirements-breeze.txt；把它們分開的原因寫在那個檔案裡。

sherpa-onnx>=1.11          # VAD（silero）；標點模型（ct-punc）也走它，但那只有 Breeze 用得到
sounddevice>=0.5           # 麥克風錄音
numpy>=1.26,<3
keyboard==0.13.5           # 全域熱鍵 hook
pywin32>=306               # 剪貼簿 / Job Object（llama-server 隨主程式同生共死）
pystray>=0.19.5            # 系統匣圖示
Pillow>=10                 # pystray 的圖示繪製
pywebview>=5.1             # 設定視窗（WebView2）
opencc-python-reimplemented==0.1.7   # 簡→繁（s2twp，含台灣用語）
huggingface_hub>=0.23      # 模型下載
requests>=2.31             # llama-server HTTP + GitHub release API
pytest>=8                  # 開發用
